// import * as tailwindcss from 'prettier-plugin-tailwindcss';

const config = {
  trailingComma: 'es5',
  tabWidth: 4,
  semi: false,
  singleQuote: true,
  // plugins: [tailwindcss],
  // tailwindFunctions: ['clsx']
};

export default config;
