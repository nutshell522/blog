// import * as tailwindcss from 'prettier-plugin-tailwindcss'

const config = {
  trailingComma: 'es5',
  tabWidth: 2,
  semi: false,
  singleQuote: true,
  // plugins: [tailwindcss],
  // tailwindFunctions: ['clsx'],
}

export default config
